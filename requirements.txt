# ── Arabic Book Brief Engine — Phase 1a (OCR) + Phase 1b (Summarisation) ──
# Streamlit Community Cloud reads requirements.txt from the repo root.
#
# PYTHON VERSION NOTE:
#   Kraken OCR requires Python 3.12 (set in Advanced settings at first deploy).
#   runtime.txt is present but Streamlit Cloud IGNORES it — version must be
#   chosen once in the Advanced settings dialog when creating the app.
#   On Python 3.14, torch has no pre-built wheels and will stall during
#   install. The app runs in "export images only" mode without Kraken.
#
# KRAKEN INSTALLATION:
#   torch, kraken, and lightning are commented out below so the app can start
#   on Python 3.14 without stalling. Uncomment them ONLY when deploying on
#   Python 3.12 (re-create the app with Python 3.12 in Advanced settings).

# Core UI
streamlit>=1.35.0

# PDF rendering
PyMuPDF>=1.24.0
pdf2image>=1.17.0        # poppler-based rendering (poppler-utils in packages.txt)

# Arabic text processing
arabic-reshaper>=3.0.0
python-bidi>=0.6.0

# Image processing (always needed for page export)
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
scipy>=1.11.0            # NLBin binarization in kraken_engine.py (optional dep)

# ── Phase 1a — Kraken OCR (Python 3.12 only) ─────────────────────────────
# Uncomment these three lines when deploying on Python 3.12.
# On Python 3.14, leave them commented — the app will offer "export images
# only" mode and skip Kraken silently.
#
# torch>=2.4.0,<=2.10.0
# lightning @ ./lightning-compat
# kraken==7.0.1

# ── Phase 1b — Summarisation & Script generation ─────────────────────────
# Diacritization (applied only to final script, not raw OCR text)
mishkal

# LLM summarization & script generation
anthropic>=0.40.0

# ── Phase 2 — Audio Generation ──────────────────────────────────────────
gTTS>=2.5.0              # free Google TTS fallback (lang='ar')
# elevenlabs>=1.0.0      # premium Arabic voices — install locally when needed

# ── Phase 3 — Visual Generation ─────────────────────────────────────────
# (No extra deps beyond those above — phase3 uses requests, Pillow, ffmpeg)
