# ── Arabic Book Brief Engine — Phase 1a (OCR) + Phase 1b (Summarisation) ──
# Streamlit Community Cloud reads requirements.txt from the repo root.
# Python 3.12 is required (set in runtime.txt / Advanced settings at deploy time).

# Core UI
streamlit>=1.35.0

# PDF rendering
PyMuPDF>=1.24.0
pdf2image>=1.17.0        # poppler-based rendering (poppler-utils in packages.txt)

# Arabic text processing
arabic-reshaper>=3.0.0
python-bidi>=0.6.0

# ── Phase 1a — Kraken OCR ────────────────────────────────────────────────
# Kraken 7.0.1 requires Python < 3.14 and pytorch-lightning.
# lightning was quarantined on PyPI 2026-04-30 — use the local shim instead.
torch>=2.4.0,<=2.10.0
lightning @ ./lightning-compat
kraken==7.0.1

# Image processing (used by Kraken pipeline and page export)
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
scipy>=1.11.0            # NLBin binarization in kraken_engine.py

# ── Phase 1b — Summarisation & Script generation ─────────────────────────
# Diacritization (applied only to final script, not raw OCR text)
mishkal

# LLM summarization & script generation
anthropic>=0.40.0

# ── Phase 2 — Audio Generation ──────────────────────────────────────────
gTTS>=2.5.0              # free Google TTS fallback (lang='ar')
# elevenlabs>=1.0.0      # premium Arabic voices — install locally when needed

# ── Phase 3 — Visual Generation ─────────────────────────────────────────
# (No extra deps beyond those above — phase3 uses requests, Pillow, ffmpeg)
